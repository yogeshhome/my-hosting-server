apiVersion: v1
kind: Service
metadata:
  name: app1-server
  namespace: production
spec:
  selector:
    app: app1-server
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP